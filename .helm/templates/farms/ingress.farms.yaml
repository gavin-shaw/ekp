apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/issuer: "letsencrypt-prod"
  name: farms-public
spec:
  tls:
    - hosts:
        - "farms.{{ .Values.ingress.hostname }}"
      secretName: ekp-public-tls
  rules:
    - host: "farms.{{ .Values.ingress.hostname }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: farms
                port:
                  number: 3001
